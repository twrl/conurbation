#pragma once

#include "ll/variant.hh"

namespace ll {

    ///
    /// A type which can have a set of arbitrary values attached to it
    ///
    //template <class TKey, class... TValues>
    class taggable_t {

    //    using key = TKey;
    //    using value = typename variant_t<TValues...>;


    };

}
